# Comments in GraphQL are defined with the hash (#) symbol.
type Registry {
  _id: String
  address: String
  solidityCode: String
  gasUsed: Int
  abi: String
  bytecode: String
  _v: Int
  processes(
    address: String
  ): [ProcessContract]
  models(
    _id: String
  ): [Model]
  policies(
    _id: String
  ): [Policy]
}
